<% if(message.length > 0){ %>
  <script>
    Swal.fire({
      title: "<%= message[1] %>",
      text: "<%= message[2] %>",
      icon: "<%= message[0] %>",
      showConfirmButton: false,
      timer: 1500
    });
  </script>
<% } %>